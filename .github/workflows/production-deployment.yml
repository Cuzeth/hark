name: Production Deployment

on:
  workflow_dispatch:
    inputs:
      what_to_test:
        description: TestFlight “What to Test” notes
        required: false
        default: Verify Google sign-in, device registration, communication notification avatars, activity history, and destination links.
        type: string

permissions:
  contents: read

concurrency:
  group: hark-production-deployment
  cancel-in-progress: false

jobs:
  deploy:
    name: Build and submit iOS app
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 20

    steps:
      - name: Check out source
        uses: actions/checkout@v5

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.10.0
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Set up Expo and EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: 21.1.0
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify monorepo
        run: |
          pnpm typecheck
          pnpm test
          pnpm lint

      - name: Verify Expo project
        working-directory: apps/expo
        run: |
          pnpm exec expo install --check
          npx expo-doctor@1.20.1

      - name: Build and submit to App Store Connect
        working-directory: apps/expo
        run: >-
          eas build
          --platform ios
          --profile production
          --auto-submit-with-profile production
          --what-to-test "${{ inputs.what_to_test }}"
          --message "Production deployment from ${{ github.sha }}"
          --non-interactive
          --no-wait
