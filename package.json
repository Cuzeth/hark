{
  "name": "hark",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @hark/website dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "teaser:send": "node scripts/send-teaser.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.5",
    "atmn": "1.1.15"
  }
}
